# ─── Vita Dashboard — variáveis de ambiente ───
#
# Copie este arquivo para .env.local (dev) ou configure na Vercel (produção)
#
# URL do backend vita-core no Railway.
# Deixe vazio em desenvolvimento (Vite proxy cuida disso).
# Exemplo produção: https://vita-core-production.railway.app
VITE_API_BASE=
